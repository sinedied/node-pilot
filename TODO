# TODO (personal scratchpad for human, not for AI agents)

- [x] rename to cockpit.js
- [x] cursor icon bug hover, flashing between regular and pointer icons
- [x] move toolbar to "Info" tab, with refresh and theme there. Should be first tab.
- [x] in the info tab, instead of "Project" show the found project name in package.json. The project name then should be removed from the list of pills.
Instead of pills, extract proper information about the project. Suggest useful info that could be displayed here, in addition to the "stack" pills, like number of dependencies with total number of included dependencies with transitives, bundle size etc. Platform (min Node.js version, package manager, etc). This extension activation relies on the presence of a package.json, so if none is present show a message that the extension is not active because no package.json was found. 
- [x] test panel: missing spacing
- [x] UI: spinner when running/loading
- [x] icons for tabs
- [x] better UI: hint on hover/click
- [x] dogfood all features
- [x] new AGENTS.md rule: ensure UI is responsive and usable at small widths
  * call /rubber-duck after each new set of changes after you're done
  * tabs: ... for extra tabs not fitting in the tab bar
- [x] integrated browser: improve integration
 + screen capture + playwright integration
- [x] test: watch switch, logs switch instead of raw output (like dev tab)
- [x] dev => preview tab
- [x] add status pills on tabs (test, problems)
- [x] settings tab:
  - [x] auto lint / test on load
  - [x] tab order and visibility (change default)
- [x] Problems tabs
  * include TS errors and lint errors, + fix me with copilot button
- [x] move theme toggle to settings tab as we now have a proper place for things like this
- [x] no disctinction in pinned script, default pinned scripts can be unpinned too
  * improve: show which scripts it's aliased to
  * order same as package.json

- [ ] bug auto load?
- [ ] clicking lint button in task list should focus the run/focus the problems tab, not the console tab

- [ ] dependencies: 
  * add link to changelog
  * scope: default or custom => manuallt check which ones to update. Default follows the package.json rules
  * audit should return more info, like which packages are vulnerable and if they have a fix available, and link to the advisories. 
  * remove the check outdated/audit buttons, should be a single refresh button for consistency with the other tabs. 
  * Safe update should become "update with Copilot" button, following the scope option selected. The behavior is to send the prompt for which packages to update and still runs all possible checks (lint, test, audit) after the update. If any of these checks fail, the update is reverted and a message is reported to the user (through Copilot) that the update failed and why. 

- [ ] debugger? + usuable by agent

- [ ] Rayfin integration (rayfin tab)


- [ ] skill readme/prep release
  - [ ] add skills (README for example)
  - [ ] logo
  - [ ] css


