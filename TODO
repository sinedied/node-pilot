# TODO (personal scratchpad for human, not for AI agents)

- [x] rename to cockpit.js
- [x] cursor icon bug hover, flashing between regular and pointer icons
- [x] move toolbar to "Info" tab, with refresh and theme there. Should be first tab.
- [x] in the info tab, instead of "Project" show the found project name in package.json. The project name then should be removed from the list of pills.
Instead of pills, extract proper information about the project. Suggest useful info that could be displayed here, in addition to the "stack" pills, like number of dependencies with total number of included dependencies with transitives, bundle size etc. Platform (min Node.js version, package manager, etc). This extension activation relies on the presence of a package.json, so if none is present show a message that the extension is not active because no package.json was found. 
- [x] test panel: missing spacing
- [x] UI: spinner when running/loading
- [x] icons for tabs
- [x] better UI: hint on hover/click
- [x] dogfood all features
- [x] new AGENTS.md rule: ensure UI is responsive and usable at small widths
  * call /rubber-duck after each new set of changes after you're done
  * tabs: ... for extra tabs not fitting in the tab bar
- [x] integrated browser: improve integration
 + screen capture + playwright integration
- [x] test: watch switch, logs switch instead of raw output (like dev tab)
- [x] dev => preview tab
- [x] add status pills on tabs (test, problems)
- [x] settings tab:
  - [x] auto lint / test on load
  - [x] tab order and visibility (change default)
- [x] Problems tabs
  * include TS errors and lint errors, + fix me with copilot button
- [x] move theme toggle to settings tab as we now have a proper place for things like this
- [x] no disctinction in pinned script, default pinned scripts can be unpinned too
  * improve: show which scripts it's aliased to
  * order same as package.json
- [x] bug auto load?
- [x] clicking lint button in task list should focus the run/focus the problems tab, not the console tab
- [x] in problems tab: icon in the header with the file name has wrong color for warning (works for errors and info)
- [x] dependencies tab: 
  * add link to changelog
  * scope: default or custom => manually check which ones to update. Default follows the package.json rules
  * audit should return more info, like which packages are vulnerable and if they have a fix available, and link to the advisories. 
  * remove the check outdated/audit buttons, should be a single refresh button for consistency with the other tabs. 
  * Safe update should become "update with Copilot" button, following the scope option selected. The behavior is to send the prompt for which packages to update and still runs all possible checks (lint, test, audit) after the update. If any of these checks fail, the update is reverted and a message is reported to the user (through Copilot) that the update failed and why. 
- [x] new debugger tab:
  * this debugger tab should be a full-featured debugger, with breakpoints, step over, step into, step out, etc. It should also have a console for evaluating expressions and inspecting variables. The debugger should be able to attach to a running process or start a new process. It should support Node.js and browser debugging.
  * Important: this debugger should be exposed to the agent, so that the agent can use it to debug code. The agent should be able to set breakpoints, step through code, and inspect variables. The agent should also be able to evaluate expressions in the console. This is the most important feature
- [x] badges disappearing or not when folded under ... tabs
- [x] monorepo handling and project selector in header
- [x] Rayfin integration (rayfin tab)
  * Search about the new Microsoft Rayfin product, and think about its purpose what could be provided in a Rayfin tab to best help users building Rayfin applications with Copilot.
- [x] auto update
- [x] Design pass: Fix with copilot button consistency, with a nice gradient border
  * Store design rules, understanding and decisions in AGENTS.md
- [x] rayfin tab:
  * if not deployed, need to input workspace name, id or URL
  * deploy button css issue on hover (all others blue buttons have this issue)
- [x] security fix with copilot: first try with audit fix (if using NPM), then if that fails, try to use copilot to fix the security issues. 
  * also layout issue, see screenshot
- [x] angular
- [x] xo linter
- [x] monorepo: turbo, rush
- [x] Readme cleanup
  * development details go to docs/development.md
  * remove how it works
  * remove roadmap section
  * remove license section (not needed already surfaced by Github)
  * Features section is too long, could you try keeping it leaner, and maybe rationalize to most important features?
- [x] rayfin real world app test

- [ ] update mechanism doesn't seem to work: investigate why, probably the GitHub release actions isn't working properly

- [x] Rayfin tab: local dev not fully implemented yet, its local dev with remote backend. Hide the local options for now (don't remove them, as they will be needed later)
  * rayfin dev commands don't exists (yet), same for "apply to local"
  * starting local server is same as starting local dev server (usually npm run dev in the templates) => should use tool from preview tab and switch to it
  * on a Fabric deployed rayfin app, only Open App link appears, no Open Fabric workspace or API endpoint link.
  * There's no way to add a new Fabric workspace if I want to switch between multiple workspaces. Once I deployed the app, I should be able to add a new workspace and switch between them.
  * dogfooding on a fake app doesn't work. I need rayfin-todo-app to be a REAL rayfin app that I can test deploy etc.
  * Set up agents files button should call the related rayfin CLI command (if it's not already the case) and the button should only be present if the agent files are not installed.
  * First rayfin header should show the currently installed version of Rayfin in the project. If there's a newer version available, it should show a notification and a button to update to the latest version using Copilot.
- [x] update: no notification at start start when update is available?
  * update: do ALL the work you can without copilot, it should be necessary to burn tokens just for an update.

- [ ] playwright tab?

- [ ] Fix security: audit ran successfully, but the prompt sent still asked to update?

- [ ] skill readme/prep release
  - [x] add skills (README for example)
  - [x] css
  - [x] logo
  - [x] readme
  - [ ] video
  - [ ] teams message devrel + pierce
  - [ ] send rayfin team
  - [ ] linkedin/x/bs



